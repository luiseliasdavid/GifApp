import { fireEvent, render,screen } from "@testing-library/react";
import { useFetchGifs } from '../src/hooks/useFetchGifs'

describe('Probando useFetchGifsapp', () => {
  test('should ', () => {
    
  })
  
})

